

export const AuthReducer = (initialState, action) => {
    switch (action.type) {
        case "value1":

            return "value1";
        case "value2":

            return "value2";

        default:
            break;
    }
};